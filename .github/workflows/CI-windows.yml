name: CI-windows

on: [push,pull_request]

jobs:

  build:
  
     runs-on: windows-latest

     steps:
     - uses: actions/checkout@v2
      
     -name: Setup msbuild.exe
      uses: warrenbuckley/Setup-MSBuild@v1
       
     - name: Build using MSBuild
       run: msbuild cppcheck.sln
